#include <iostream>

using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  int tt;
  cin >> tt;
  while (tt--) {
    int n, m;
    cin >> n >> m;
    cout << n - 1 << '\n';
    while (m--) {
      int foo, bar;
      cin >> foo >> bar;
    }
  }
  return 0;
}