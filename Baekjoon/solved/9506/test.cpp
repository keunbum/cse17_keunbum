#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  const int MAX = (int) 1e5;
  for (int i = 3; i < MAX; i++) {
    cout << i << '\n';
  }
  cout << -1 << '\n';
  return 0;
}
