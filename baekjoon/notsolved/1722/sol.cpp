#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  int n;
  cin >> n;
  vector<int> a(n);
  iota(a.begin(), a.end(), 0);
  int foo;
  cin >> foo;
  if (foo & 1) {
    long long k;
    cin >> k;
    while (k--) {
    }
  } else {
    
  }
  return 0;
}
